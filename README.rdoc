App:

- Uses standard queries
- Uses aggregation pipeline queries
- Creates an index and uses geolocation queries
- Uses GridFS file storage and retrieval
Serves content to the web:
- Has ability to manage places and photos and associate photos with a nearby place
- View places and their associated photos
